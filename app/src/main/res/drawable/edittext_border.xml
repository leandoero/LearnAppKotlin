<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Прозрачный фон -->
    <solid android:color="@android:color/transparent" />

    <!-- Обводка (1dp, серый цвет) -->
    <stroke
        android:width="1dp"
        android:color="#888888" />
</shape>